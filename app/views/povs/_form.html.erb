<%= form_with(model: pov) do |form| %>
  <% if pov.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pov.errors.count, "error") %> prohibited this pov from being saved:</h2>

      <ul>
        <% pov.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>

  <div class="field">
    <%= form.label :publication_year %>
    <%= form.number_field :publication_year %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
